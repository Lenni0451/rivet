plugins {
    id "project.defaults"
}

repositories {
    maven {
        name = "Lenni0451"
        url = "https://maven.lenni0451.net/everything"
    }
}

dependencies {
    api project(":")
    api (libs.thingl) {
        exclude group: "org.jetbrains.kotlin"
    }
    api libs.bundles.lwjgl
}
